# govulncheck CI gate for the ledger service (services/ledger). Reachability-
# aware CVE scan of the transitive dependency set. Relocated from the standalone
# ledger repo and converted to a run-based step (mirrors the witness go-test
# vuln job) so it cd's into the module and tolerates a restricted-egress runner.
name: ledger-govulncheck

on:
  push:
    branches: [main]
    paths:
      - "services/ledger/**"
      - ".github/workflows/ledger-govulncheck.yml"
  pull_request:
    paths:
      - "services/ledger/**"
      - ".github/workflows/ledger-govulncheck.yml"
  schedule:
    # Daily scan so a newly-published CVE in a transitive dep is flagged within
    # 24h even with no PR activity.
    - cron: "23 6 * * *"

permissions:
  contents: read

defaults:
  run:
    working-directory: services/ledger

jobs:
  govulncheck:
    name: govulncheck
    runs-on: ubuntu-latest
    # Scan UPSTREAM (GOWORK=off): the baseproof fork is upstream v1.0.2 + a
    # deterministic-shutdown patch, so the transitive-dependency CVE surface is
    # identical, and this avoids needing the third_party/tessera submodule.
    env:
      GOWORK: "off"
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6.0.3
        with:
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c  # v6.4.0
        with:
          go-version-file: services/ledger/go.mod

      # Fail only on a real finding (govulncheck exit code 3). Treat an
      # operational failure (DB unreachable on a restricted runner) as a warning
      # so an egress policy can't silently mask the gate.
      - name: govulncheck
        run: |
          set +e
          go run golang.org/x/vuln/cmd/govulncheck@latest ./...
          code=$?
          set -e
          if [ "$code" -eq 3 ]; then
            echo "::error::govulncheck found vulnerabilities"
            exit 1
          fi
          if [ "$code" -ne 0 ]; then
            echo "::warning::govulncheck did not complete (exit $code) — runner likely cannot reach vuln.go.dev; not failing the job"
          fi
