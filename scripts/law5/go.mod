// Standalone checker module for LAW 5 (scripts/dependency-law.sh). Stdlib only;
// deliberately outside the repo go.work (the law runs with GOWORK=off).
module github.com/baseproof/tooling/scripts/law5

go 1.25.11
