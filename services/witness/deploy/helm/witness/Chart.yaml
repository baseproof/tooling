apiVersion: v2
name: witness
description: Baseproof standalone witness — a blind-notary K-of-N tree-head cosigner.
type: application

# Chart version. Bump on every chart change.
version: 0.1.2

# Tracks the witness image tag this chart's defaults target.
# Override per-deploy via .Values.image.tag.
appVersion: "0.1.6"

kubeVersion: ">= 1.23.0"

home: https://github.com/baseproof/tooling
sources:
  - https://github.com/baseproof/tooling

maintainers:
  - name: clearcompass-ai

annotations:
  artifacthub.io/license: Apache-2.0
  artifacthub.io/category: security
