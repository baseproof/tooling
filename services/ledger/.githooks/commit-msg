#!/bin/bash

COMMIT_MSG_FILE=$1

if grep -qiE "claude\.ai/code|generated by claude|🤖 generated with" "$COMMIT_MSG_FILE"; then
    echo "❌ ERROR: Commit message contains blocked AI clutter (Claude URLs or specific watermarks)."
    echo "The commit has been rejected. Please edit your message and try again."
    exit 1
fi